import Animated from 'react-native-reanimated';
import styled from 'styled-components/native';

export const SearchInfoContainer = styled(Animated.View)``;

export const SearchInfoButton = styled.TouchableOpacity`
  display: flex;
  flex-direction: row;
  width: 100%;
  padding: 10px 0;
`;